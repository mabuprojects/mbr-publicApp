import { DataAppDirective } from './data-app.directive';

describe('DataAppDirective', () => {
  it('should create an instance', () => {
    const directive = new DataAppDirective();
    expect(directive).toBeTruthy();
  });
});
